spring:
  h2:
    console:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
  data:
    jpa:
      repositories:
        bootstrap-mode: default
  cloud:
    discovery:
      enabled: false
